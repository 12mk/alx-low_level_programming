#include "main.h"
/**
*print_alphabet_x10 - the function print out alphabet
*Return: return 0 value
*/
void print_alphabet_x10(void)
{
 	char i;
	int x;

	for(x = 0; x <= 9; x++)
	{
	for (i = 'a'; i <= 'z'; i++)
	{
	_putchar(i);
	}
	_putchar('\n');
	}
	return(0);
}

